# Agent Notes

This project is a standalone TanStack Start app. It has no runtime
dependency on Lovable and can be developed, built, and deployed with
`bun install && bun run build` on any host that runs a Nitro
Cloudflare Worker bundle (or swap the `preset` in `vite.config.ts` for
`node-server`, `vercel`, `netlify`, etc.).
